# 环境配置
appium:
  server_url: http://localhost:4723

timeouts:
  implicit_wait: 5
  explicit_wait: 10
  action_interval: 0.5

retry:
  max_attempts: 3
  interval: 1

screenshot:
  on_failure: true
  save_path: artifacts/screenshots
